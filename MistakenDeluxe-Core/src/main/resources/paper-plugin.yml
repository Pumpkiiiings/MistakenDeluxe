name: Mistaken
version: Deluxe-2.0
main: liric.mistaken.Mistaken # <-- ¡Ojo! Asegúrate que coincida con tu package
loader: liric.mistaken.MistakenPluginLoader
api-version: '1.21'
author: Pumpkingz

# Esto es lo que arregla el error de Vault y LuckPerms, pariente
dependencies:
  server:
    Meowtils:
      load: BEFORE
      required: true
    Vault:
      load: BEFORE
      required: true
    LuckPerms:
      load: BEFORE
      required: true
    PlaceholderAPI:
      load: BEFORE
      required: false
    CraftEngine:
      load: BEFORE
      required: false
    packetevents:
      load: BEFORE
      required: true
